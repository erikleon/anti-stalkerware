// Intentionally empty for now. IPC bridges get added here as the renderer
// needs them, each one backed by a handler registered through
// registerHandler or registerGated in ipc.ts — never exposed directly.
